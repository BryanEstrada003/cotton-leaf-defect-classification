# Construir la imagen Docker
docker build -t single .

# eliminar la imagen docker
docker rmi single

# Detener y eliminar el contenedor existente
docker stop single
docker rm single

# Log in to Docker
docker login

# Lo mismo que el anterior solo que le asigno un puerto para poder trabajar con jupyter notebook
docker run -it -p 8889:8888 --gpus all -v "c/Users/Admin-Cidis/Documents/mai/cotton-leaf-defect-classification:/app" --name ai-project single

# Verificar la versión de nvcc
nvcc --version

# Verificar la instalación de CUDA
nvidia-smi

# Detener el contenedor Docker
docker stop single

# Iniciar el contenedor detenido
docker start single

# Adjuntar al contenedor en ejecución
docker attach single

# O abrir una nueva sesión de shell en el contenedor
docker exec -it single /bin/bash

#abrir jupyter
jupyter notebook --ip=0.0.0.0 --port=8888 --allow-root